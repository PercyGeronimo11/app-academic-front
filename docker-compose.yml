version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "88:80"
    environment:
      - VITE_API_URL=http://161.132.40.114:89/api
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
